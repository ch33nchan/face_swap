[project]
name = "char-swap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fal-client>=0.13.0",
    "gradio>=6.5.1",
    "python-dotenv>=1.2.1",
]
